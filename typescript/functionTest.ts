function square(number: number): number {
  return number * number;
}
console.log(square(2));

function exponentNumber(number: number): number {
  return number * number * number;
}
console.log(exponentNumber(3));
